#include <fstream>
#include <iostream>
#define REP(i, n) for (int i = 0; i < (int) (n); i++)
using namespace std;

int main(int argc, char* argv[]) {
    ifstream out(argv[3]);
    ifstream ans(argv[2]);
    ifstream in(argv[1]);
}
