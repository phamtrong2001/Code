#include <bits/stdc++.h>

using namespace std;

int main()
{
    freopen("INP.TXT", "r", stdin);
    cin >> n >> s;
    for (int i = 1; i <= n; i++)
    {
        cin >> a[i].x >> a[i].v >> a[i].t;
        if (a[i].t == 1)
        {
            lef = min(lef, a[i].x / a[i].v);
            dplef[a[i].x] = ()
        if (a[i].t == 2) rig = min(rig, (M - a[i].x)/a[i].v);

    }
}
