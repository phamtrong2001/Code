#include <bits/stdc++.h>
#define mh 5005
using namespace std;
int n, a[mh], x, y;
int main ()
{
    freopen( "tree.inp","r",stdin); freopen( "tree.out","w",stdout);
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin >> n;
    for ( int i=1; i<=n; i++ )
        cin >> a[i];
    for ( int i=1; i<=n-1; i++ )
        cin >> x >> y;
        cout << "1";
    }

