#include<bits/stdc++.h>
#define task "bubbletea"
#define ll long long
#define ff(i,a,b) for(int i=a; i<=b; ++i)
#define fd(i,a,b) for(int i=a; i>=b; --i)
#define rr(i,a,b) for(int i=a; i<b; ++i)
#define rd(i,a,b) for(int i=a; i>b; --i)
#define pb push_back
#define mp make_pair
using namespace std;

const int maxn=1E5+10;

int par[maxn], m, n, vuiChoi[maxn];
vector<int> e[maxn];
vector<ll> c[maxn];
ll f[maxn], kq;

void dfs(int x, int sl)
{
	
}

int main()
{
	freopen(task".inp","r",stdin);
	freopen(task".out","w",stdout);
	
	cout<<-1;
	
	return 0;
}
